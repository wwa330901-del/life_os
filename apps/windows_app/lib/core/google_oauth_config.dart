/// Public OAuth client ID from Google Cloud Console (Credentials -> OAuth
/// client, application type "Desktop app"). This is not a secret — the
/// matching client_secret lives only in the backend's .env and is never
/// shipped in this app.
const googleOAuthClientId =
    '278218437099-ef1t34s5gr53pgfcovmdmgspo9dvfq72.apps.googleusercontent.com';
